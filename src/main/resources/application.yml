spring:
  application:
    name: FabLab
  datasource:
    url: jdbc:postgresql://localhost:5432/fablab
    username: postgres
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect